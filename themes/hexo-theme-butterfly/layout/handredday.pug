extends includes/layout.pug

block content
  include ./includes/mixins/article-sort.pug
  #archive
    .article-sort-title= _p('page.articles') + ' - ' + site.posts.filter(post => post.hide).length
    +articleSort(page.posts)
    include includes/pagination.pug